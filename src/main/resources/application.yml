spring:
  datasource:
    # 数据源基本配置
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/mybatis_demo
    type: com.alibaba.druid.pool.DruidDataSource
#    initialization-mode: always
#    schema=classpath:
#      - classpath:sql/department.sql
#      - classpath:sql/employee.sql
    # 数据源其他配置
    initialSize: 5
    minIdel: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    # 配置监控统计链接的filters，去掉后监控界面sql无法统计，"wall"用于防火墙
    filters: stat,wall,slf4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druia.stat.mergeSql=true;druid.stat.slowSqlMillis=500
#    druid:
#      initial-size: 5
#      min-idle: 5
#      max-active: 20
#      max-wait: 60000
#      time-between-eviction-runs-millis: 60000
#      min-evictable-idle-time-millis: 300000
#      validation-query: SELECT 1 FROM DUAL
#      test-while-idle: true
#      test-on-borrow: false
#      test-on-return: false
#      pool-prepared-statements: true
#      # 配置监控统计链接的filters，去掉后监控界面sql无法统计，"wall"用于防火墙
#      filters: stat,wall,log4j
#      max-pool-prepared-statement-per-connection-size: 20
#      use-global-data-source-stat: true
#      connection-properties: druia.stat.mergeSql=true;druid.stat.slowSqlMillis=500
